#!/usr/bin/env bash
set -eou pipefail

cd $(git rev-parse --show-toplevel)

source scripts/env

echo "+++ Hadolint +++"

for dockerfile in docker/Dockerfile*; do
  echo "Checking $dockerfile"
  docker run ${COMMON_RUN_ARGS[*]} \
             ${REGISTRY_DOCKER}/hadolint/hadolint < $dockerfile
done
