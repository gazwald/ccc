#!/usr/bin/env bash
set -eou pipefail

cd $(git rev-parse --show-toplevel)

source scripts/env

echo "+++ Shellcheck +++"

docker run ${COMMON_RUN_ARGS[*]} \
           --mount "type=bind,src=${PWD},dst=/mnt" \
           ${REGISTRY_DOCKER}/koalaman/shellcheck:stable $(find ./scripts -type f)
